#pragma once

#include <vector>
#include <string>
#include <algorithm>


class CBuffer
{
public:
    static const size_t kCheapPrepend = 8;//可以放这次解析的长度
    static const size_t kInitialSize = 1024;
    explicit CBuffer(size_t initila)
private:





};